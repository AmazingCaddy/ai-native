{
  "name": "ai-native",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear",
    "assistant:dev": "npm run dev --workspace @ai-native/assistant",
    "assistant:build": "npm run build --workspace @ai-native/assistant",
    "assistant:experiment:model-comparison": "npm run experiment:model-comparison --workspace @ai-native/assistant --",
    "assistant:start": "npm run start --workspace @ai-native/assistant",
    "assistant:test": "npm run test --workspace @ai-native/assistant",
    "assistant:typecheck": "npm run typecheck --workspace @ai-native/assistant"
  },
  "dependencies": {
    "@docusaurus/core": "^3.7.0",
    "@docusaurus/preset-classic": "^3.7.0",
    "@docusaurus/theme-mermaid": "^3.7.0",
    "@mermaid-js/layout-elk": "^0.1.7",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=24.0"
  }
}
